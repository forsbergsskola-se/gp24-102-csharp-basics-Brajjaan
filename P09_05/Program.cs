Console.WriteLine("give me a radius");

float radius = float.Parse(Console.ReadLine());   

float area = (float)(Math.PI * Math.Pow(radius, 2)); //rider autocomplete? xD, so why not?

Console.WriteLine("area is {0}", area);